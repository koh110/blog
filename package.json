{
  "private": true,
  "name": "blog",
  "main": "index.js",
  "workspaces": [
    "astro"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.14.1"
  }
}
